<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.beto.movil_practica">

    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name=".Movil_practica">
        <activity android:name=".Client_list"></activity>
        <activity android:name=".Home">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".VentasActivity" />
        <activity
            android:name=".PopUp"
            android:theme="@style/popupStyle" />
        <activity android:name=".ClienteActivity" />
        <activity android:name=".ProductoActivity" />
        <activity android:name=".ConsultasActivity" />
        <activity android:name=".PopUpVenta" />

        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="com.example.beto.movil_practica.fileProvider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/path_provider" />
        </provider>

        <activity android:name=".AddClient" />
        <activity android:name=".AddProduct" />
        <activity android:name=".BuyProduct" />
    </application>

</manifest>